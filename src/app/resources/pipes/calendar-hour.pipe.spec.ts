import { CalendarHourPipe } from './calendar-hour.pipe';

describe('CalendarHourPipe', () => {
  it('create an instance', () => {
    const pipe = new CalendarHourPipe();
    expect(pipe).toBeTruthy();
  });
});
