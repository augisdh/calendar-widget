import { CalendarDayNamePipe } from './calendar-day-name.pipe';

describe('CalendarDayNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CalendarDayNamePipe();
    expect(pipe).toBeTruthy();
  });
});
